import { CHAINLINK_TOKEN_ADDRESS_SEPOLIA } from "./consts";

const contractAddresses: Record<string, Record<string, string>> = {
  sepolia: {
    chainlinkToken: CHAINLINK_TOKEN_ADDRESS_SEPOLIA,
  },
};

export default contractAddresses;
